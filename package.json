{
  "name": "fcc-commercial-trainer",
  "private": true,
  "description": "Practice questions with spaced repetition for the FCC commercial radio operator exams (GROL, MROP, Ship Radar)",
  "license": "MIT",
  "scripts": {
    "lint": "eslint",
    "prepare": "git config core.hooksPath .githooks",
    "test": "node tests/validate-bank.js && node tests/fsrs-test.js && node tests/readiness-test.js",
    "test:coverage": "c8 --exclude tests --exclude tools --exclude dist --reporter=text --reporter=text-summary npm test",
    "test:browser": "tests/run-browser.sh",
    "serve": "node tests/serve.mjs 8081"
  },
  "devDependencies": {
    "@eslint/js": "^10",
    "@playwright/test": "^1.62",
    "c8": "^12",
    "eslint": "^10",
    "globals": "^17"
  }
}
