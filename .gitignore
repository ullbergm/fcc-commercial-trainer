# The NC CDL manual is © AAMVA (reproduction licensed to state DMVs only).
# Download it from NCDMV — see the link in README.md.
commercial-driver-manual.pdf

# dev tooling
node_modules/

# throwaway copy of index.html left behind if generate.sh is interrupted
.screenshot-page.html
